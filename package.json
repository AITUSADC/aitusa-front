{
  "name": "aitusa",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm run dev:web\" \"npm run dev:api\"",
    "dev:web": "npm run dev --workspace=@aitusa/web",
    "dev:api": "npm run dev --workspace=@aitusa/api",
    "build": "npm run build --workspaces --if-present",
    "start": "concurrently -n web,api -c blue,green \"npm run start --workspace=@aitusa/web\" \"npm run start --workspace=@aitusa/api\"",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
