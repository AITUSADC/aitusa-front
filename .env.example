# Copy this file to apps/api/.env when the API runs directly on the host.
DATABASE_URL=postgresql://aitusa:aitusa@localhost:5432/aitusa?schema=public
API_PORT=3001
ADMIN_API_KEY=replace-with-a-long-random-value

# Optional Next.js override (the same URL is already the local default).
API_URL=http://localhost:3001
